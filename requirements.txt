# ---------------------------------------------------------------------------
# Requisitos del proyecto (pensamiento / vision / voz / main)
# Instalar con: pip install -r requirements.txt
# ---------------------------------------------------------------------------

# Núcleo de deep learning
tensorflow==2.17.*
tf-keras==2.17.*        # puente Keras 2 requerido por los modelos TF de transformers

# Transformers (ViT para vision.py, Wav2Vec2 para voz.py)
transformers>=4.40,<5

# Visión artificial
opencv-python>=4.9
pillow>=10.0             # requerido internamente por ViTImageProcessor

# Utilidades numéricas
numpy>=1.26,<2.0

# --- Opcionales: solo si vas a usar TextToSpeechSynthesizer (voz.py) ---
# soundfile>=0.12
# git+https://github.com/TensorSpeech/TensorFlowTTS.git
